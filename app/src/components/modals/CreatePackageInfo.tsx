'use client'

import { DialogContent, DialogDescription, DialogHeader, DialogTitle } from "../ui/dialog";

export default function CreatePackageInfo() {
  return (
    <DialogContent>
    <DialogHeader>
      <DialogTitle>Create Package Info</DialogTitle>
      <DialogDescription>
        <div>
          <textarea disabled className="w-full">
            wkfoakfoakfokafkwaofkawof
          </textarea>
        </div>
      </DialogDescription>
    </DialogHeader>
  </DialogContent>
  );
}
